<%= render 'homepage/dueling/card_information' %>

<% ["graveyard", "banishment", "deck", "extra_deck"].each do |name| %>
  <div data-dueling-board-target="<%= "#{name}-overlay" %>" class="p-3 hidden-overlay board-overlay">
    <button class="btn btn-danger px-3" data-action="click->dueling-board#closeBoardOverlay">X</button>
    <h1 class="text-center"><%= name.titleize %></h1>
    <div data-dueling-board-target="<%= "#{name}-overlay-cards" %>" class="d-flex flex-row justify-content-evenly flex-wrap">

    </div>
  </div>
<% end %>