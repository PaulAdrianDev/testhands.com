<section class="container-fluid">
  <%= link_to "< Back", user_path(@user), class: "btn btn-primary  mb-2" %>
  <h1 class="mb-3">Change your details:</h1>
  <%= form_with model: @user, url: user_path(@user) do |form| %>
    <div class="mb-3">
      <%= form.label :username, "Username:"%>
      <br>
      <%= form.text_field :username, placeholder: "Insert new username", value: @user.username, class: "px-2 rounded-2" %>
    </div>
    <div class="mb-3">
      <%= form.label :email_address, "Email Address:"%>
      <br>
      <%= form.email_field :email_address, placeholder: "Insert new email", value: @user.email_address, class: "px-2 rounded-2" %>    
    </div>
    <%= render 'shared/form_errors' %>
    <%= form.submit "Update", class: "btn btn-success px-3" %>
  <% end %>
</section>